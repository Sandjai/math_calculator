
export interface Imodes {
    runtime: string, 
    constructor: string,
    [key: string]: string
  }
  
  export interface IcanvasSizes {
    width: number, 
    height: number,
  }
  export interface IelementsSettings{
    id: string,
    width: number,
    height: number,
    rows: number,
    columns: number,
    data: string[]
    }